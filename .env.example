# İzletiriz — environment variables
#
# All AI features (Thumbnail "AI Background", AI video script + voice-over)
# work WITHOUT a key using offline/browser fallbacks. Set this key to enable
# real AI generation. On Vercel: Project Settings → Environment Variables.

# Optional — enables real AI: image background, LLM script, TTS voice-over.
OPENAI_API_KEY=
